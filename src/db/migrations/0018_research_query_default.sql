-- Этап 13: поправка дефолтного поискового запроса.
--
-- Первая же проверка на живом поиске показала, что кавычки вокруг названия и три
-- уточняющих слова обнуляют выдачу: у малоизвестного проекта просто нет страниц,
-- где «merabo.ru», «обман» и «вывод денег» стоят рядом в точной форме. Кавычки убираем.
--
-- Правим только то значение, которого клиент не касался: если запрос уже подобран
-- вручную, перезаписывать его миграцией нельзя.
UPDATE settings
   SET value = '{{проект}} отзывы обман вывод денег', updated_at = now()
 WHERE key = 'research_query'
   AND value = '"{{проект}}" отзывы обман вывод денег';

-- Два источника со своим кодом обнаружения (этап 5).
--
-- Оба сайта не отдают карту сайта, поэтому строкой в БД тут не обойтись — у каждого свой
-- адаптер в `src/sources/adapters/`. Режим `discovery = 'custom'` включает его по коду
-- источника, поля `sitemap_*` для них не используются.
--
-- brokertribunal.com — карта есть, но пустая (`<urlset/>` без записей), а раздел
-- `/brokers` отвечает 404. Обзоры при этом живые: адаптер снимает листинги
-- `/brokers/overviews` и `/investment-projects/overviews` — те же страницы, по которым
-- ходит посетитель. Дата у карточки лежит в HTML-комментарии: сайт перестал её выводить,
-- но из разметки не убрал. Режим `text` — на странице обзора около 9000 символов.
--
-- sikayetvar.com — площадка жалоб, `/sitemap.xml` отвечает 410. Обнаружение идёт по
-- русской ленте `/ru/complaints`. Режим `topic_only`: жалоба — короткий текст
-- пользователя, рерайтить там нечего, темой становится бренд. Лента общая на весь сайт,
-- поэтому отсев по денежным словам живёт в адаптере.
--
-- Приоритет 10 у обоих: клиент просил считать новые источники главными.

INSERT INTO sources (code, title, base_url, discovery, content_mode, fetch_via,
                     priority, is_active, notes)
VALUES
  ('brokertribunal',
   'Broker Tribunal — обзоры брокеров и инвестпроектов',
   'https://brokertribunal.com',
   'custom',
   'text',
   'firecrawl',
   10,
   true,
   'Свой адаптер: карта сайта пустая, обнаружение по листингам /brokers/overviews и /investment-projects/overviews. Дата берётся из HTML-комментария в карточке.'),
  ('sikayetvar',
   'Sikayetvar — жалобы пользователей (русский раздел)',
   'https://www.sikayetvar.com',
   'custom',
   'topic_only',
   'direct',
   10,
   true,
   'Свой адаптер: карты сайта нет (410), обнаружение по ленте /ru/complaints. Только тема — темой становится бренд, на который жалуются. Отсев по денежным словам в адаптере.')
ON CONFLICT (code) DO NOTHING;

# Дорожная карта — перенос под клиента Комарова

Это **не разработка с нуля**, а перенос готовой системы автопостинга (проект AlexNef,
`Downloads/ЗАКАЗЫ/posting_vk/vkposter`) под второго клиента. Код продукта уже написан и
проверен на проде: 14 этапов исходной разработки описаны в
[`07_ROADMAP_PRODUCT.md`](07_ROADMAP_PRODUCT.md) — этот файл читать как справочник
по устройству системы, чекбоксы там относятся к первому клиенту и здесь не действуют.

Что реально меняется при переносе:

1. **Другой сервер.** Первый клиент живёт на выделенном VPS со своим Postgres и своим
   Caddy. Этот проект подселяется на общий сервер okhost (`193.17.95.54`), где Postgres,
   Redis и Caddy — общие на всех клиентов. Значит: свой `postgres` из compose убрать,
   подключение брать из `.env.infra`, порты наружу не публиковать, задать `mem_limit`.
2. **Другие ключи и другой проект в postmypost** (`project_id 345029`), другие группы —
   9–10 в ВК плюс Одноклассники.
3. **Четыре новых источника** сверх шести имеющихся. Два берутся картой сайта, двум
   нужны свои адаптеры (карта пустая / структура нестандартная).
4. **Другой домен** — `vktop545.com`.

> **Где смотреть прогресс**: [`STATUS.md`](STATUS.md). Внутри каждого этапа ниже —
> чекбоксы acceptance-пунктов. Закрытие этапа сопровождается тегом `stage-N-done`.
>
> **Правило**: за один заход — один этап. Человек верифицирует его вживую, только после
> этого ставится тег и берётся следующий.

| # | Этап | Главный артефакт | Зависит от |
|---|---|---|---|
| 0 | Копия репозитория и новая идентичность | Проект собирается и стартует локально под именем `komarov-vkposter`, репо `vkposter-komarov-` | — |
| 1 | Адаптация под общий сервер okhost | `docker-compose.yml` без своего Postgres, подключение по `DATABASE_URL`, `mem_limit`, деплой-воркфлоу | 0 |
| 2 | Развёртывание и домен | Панель открывается по `https://vktop545.com`, миграции применены, `/health` зелёный | 1 |
| 3 | Ключи клиента и группы | В панели видны 9–10 групп ВК и Одноклассники из проекта postmypost 345029 | 2 |
| 4 | Новые источники по карте сайта | `bookmaker-ratings.ru` и `zarabota.com` дают свежие материалы с текстом | 3 |
| 5 | Свои адаптеры под два источника | `brokertribunal.com` и `sikayetvar.com` дают темы в очередь | 4 |
| 6 | Живой прогон и приёмка | Прогон по расписанию публикует посты во все группы клиента | 5 |

---

## Этап 0 — Копия репозитория и новая идентичность

**Цель**: рабочая копия продукта в отдельном репозитории, ничем не связанная с проектом
первого клиента, — чтобы правки под Комарова не уезжали к AlexNef и наоборот.

**Что делаем**:
- Копия кода в `D:\claude\zakazi0101010\vk_soft_post\vkposter` без `node_modules`,
  `.git`, `delivery/`, `.secrets/`, `.env` — секреты и артефакты первого клиента не переносим.
- Свой git-репозиторий, ветка `dev`, remote `https://github.com/dimatolpygin/vkposter-komarov-`.
- Переименование идентичности проекта: имя БД, пользователь БД, имя контейнера,
  домен в конфигурации — везде `komarov-vkposter` / `vktop545.com`.
- `.env` из доступов клиента (`../dostupi.txt`): postmypost, firecrawl, kie.ai, openrouter.
- Дорожная карта и STATUS переноса; исходные — в `*_PRODUCT.md` как справочник.

**Критерии приёмки** (закрытие → tag `stage-0-done`):
- [x] `git log` в новой папке показывает свою историю, `origin` указывает на `vkposter-komarov-`
- [x] В репозитории нет ни одного файла с ключами первого клиента (`git grep` по старым токенам пуст)
- [x] Локальный `docker compose up` поднимает приложение, `/health` отдаёт 200
- [x] Вход в панель по логину/паролю из нового `.env` работает

---

## Этап 1 — Адаптация под общий сервер okhost

**Цель**: проект соответствует правилам общего сервера — не поднимает свою БД, не
публикует порты, знает свой потолок памяти.

**Разведано**: `okhost new` кладёт в `.env.infra` не только `DATABASE_URL`, но и
`POSTGRES_HOST/PORT/DB/USER/PASSWORD` — ровно те переменные, которые приложение уже читает.
Значит **код подключения к БД трогать не нужно**, достаточно убрать из compose жёстко
прописанный `POSTGRES_HOST: postgres` и подключить `.env.infra` через `env_file`.

**Что делаем**:
- `docker-compose.yml` становится боевым файлом для okhost: без своего `postgres`,
  без `ports:`, `container_name: komarov-vkposter-app`, `mem_limit`, `cpus`,
  сети `data` и `edge` как external, `env_file: [.env.infra, .env]` (порядок важен —
  значения проекта перекрывают серверные).
- Dev-слой переезжает из `docker-compose.override.yml` в `docker-compose.dev.yml`:
  override подхватился бы автоматически и на сервере, увезя в прод свой Postgres,
  публикацию порта и монтирование кода. Локальный Postgres назван `infra-postgres`,
  как общий серверный, — топология одна и та же.
- Удаляем то, что относилось к схеме выделенного VPS: `docker-compose.prod.yml`,
  `deploy/Caddyfile`, `deploy/backup-db.sh` (бэкап делает `okhost backup`),
  `scripts/install.sh` (сервер уже подготовлен).
- `deploy/deploy.sh` переписан под один файл; проверка `/health` идёт изнутри
  контейнера — наружу порт не опубликован.
- `.github/workflows/deploy.yml`: каталог `/opt/projects/komarov-vkposter`,
  проверка по `https://vktop545.com`.

**Критерии приёмки** (закрытие → tag `stage-1-done`):
- [x] В `docker-compose.yml` нет сервисов `postgres`, `redis` и ни одного `ports:`
- [x] У сервиса `app` заданы `container_name: komarov-vkposter-app`, `mem_limit`, `cpus`
- [x] Подключение к БД приходит из `.env.infra`, в самом compose реквизитов нет
- [x] Локальный запуск двумя файлами по-прежнему работает: `/health` 200, вход в панель

---

## Этап 2 — Развёртывание и домен

**Цель**: система живёт на сервере и доступна клиенту по HTTPS.

**Что делаем**:
- `okhost new komarov-vkposter <repo>` + `okhost clone` — БД, пользователь, `.env.infra`.
- Залить `.env` через `scp`, права 600.
- `okhost rebuild`, проверить логи: миграции применились, крэш-лупа нет.
- `okhost web komarov-vkposter vktop545.com 3000` — домен и сертификат.
- CI/CD: секреты репозитория, пробный выкат в `master`.

**Критерии приёмки** (закрытие → tag `stage-2-done`):
- [x] `okhost list` показывает проект со своей БД и доменом
- [x] `https://vktop545.com` открывает форму входа, сертификат валиден
- [x] `/health` отдаёт 200 с версией миграций и `db: ok`
- [ ] Push в `master` доезжает до сервера: `git rev-parse HEAD` совпадает, в логах новая версия

---

## Этап 3 — Ключи клиента и группы

**Цель**: система работает с проектом клиента в postmypost и знает его сообщества.

**Что делаем**:
- Проверить связку с postmypost (`project_id 345029`) на живом запросе.
- Синхронизация групп в панели: подтянуть 9–10 сообществ ВК и Одноклассники.
- Проставить постов в день на каждую группу, включить нужные.
- Проверить публикацию в режиме черновика: пост появляется в проекте postmypost.

**Критерии приёмки** (закрытие → tag `stage-3-done`):
- [x] В разделе «Группы» видны все сообщества клиента с сетью (ВК / ОК) — синхронизация
  вернула 9 групп ВК, все `connection_status = 1`; Одноклассников в проекте нет
- [x] Включённые группы имеют заданное число постов в день — все 9 включены,
  по 10 постов (значение по умолчанию, ждёт подтверждения клиента)
- [ ] Одиночный пост в режиме `draft` доходит до postmypost и виден клиенту
- [x] Обложка поста скачивается postmypost по адресу `https://vktop545.com/...` —
  проверено живьём: postmypost сделал HEAD и GET на `/media/2026-08/...` (200)
  и выдал `file_id 52076870`

---

## Этап 4 — Новые источники по карте сайта

**Цель**: два новых сайта дают материал штатным механизмом, без своего кода.

**Разведано заранее**:
- **bookmaker-ratings.ru** — WordPress, `sitemap_index.xml` открыт. Обзоры букмекеров
  лежат в `bookreviews-sitemap.xml` (511 адресов вида `/review/...`). WP REST API отвечает,
  но кастомный тип `bookreviews` в нём не опубликован (`rest_base` только `posts`/`pages`) —
  значит тексты идут не через WP API, а прямым запросом либо через firecrawl.
- **zarabota.com** — Blogger, а не WordPress. Карта сайта — индекс из страниц вида
  `sitemap.xml?page=1`, имён-паттернов (`post-sitemap`) там нет: фильтр по паттерну
  для этого источника должен отключаться, а не отсекать всё.

**Что делаем**:
- Миграция с двумя записями в `sources` (код, база, карта, паттерн, режим доступа).
- Поправка `sources/sitemap.js` под индекс без имён-паттернов (Blogger).
- Проверка кнопкой «Проверить источник» в панели.

**Критерии приёмки** (закрытие → tag `stage-4-done`):
- [ ] Проверка `bookmaker-ratings` кладёт в `articles` свежие `/review/...` с датами
- [ ] Проверка `zarabota` кладёт в `articles` свежие материалы с датами
- [ ] У материалов обоих источников заполнен `content` (текст, а не пустое поле)
- [ ] Старые шесть источников продолжают работать: проверка всех не даёт новых ошибок в журнале

---

## Этап 5 — Свои адаптеры под два источника

**Цель**: два сайта, которые не отдают карту сайта, всё равно поставляют темы.

**Разведано заранее**:
- **brokertribunal.com** — `sitemap.xml` существует, но `<urlset>` в нём **пустой**;
  `/brokers` отдаёт 404. Статьи вида `/brokers/<name>/overview` открываются напрямую (200).
  Нужен адаптер: найти рабочий листинг брокеров и снимать его.
- **sikayetvar.com** — площадка жалоб. На странице бренда (`/ru/pocket-option-ru`) лежит
  список жалоб `/ru/<бренд>/<слаг-жалобы>`. Ближайший аналог в коде — адаптер `scama`:
  «только тема», темой становится жалоба.

**Что делаем**:
- `src/sources/adapters/brokertribunal.js` и `src/sources/adapters/sikayetvar.js`,
  регистрация в `src/sources/discovery.js`.
- Миграция с двумя записями `sources` в режиме `custom`.
- Паузы между запросами: обход — залп по чужому хосту, на vklader это уже давало 403 по IP.

**Критерии приёмки** (закрытие → tag `stage-5-done`):
- [ ] Проверка `brokertribunal` даёт непустой список тем с датами
- [ ] Проверка `sikayetvar` даёт непустой список тем с датами
- [ ] Повторная проверка не создаёт дублей (дедуп по URL и по теме работает)
- [ ] Ни один адаптер не роняет прогон: сбой источника уходит в журнал ошибок

---

## Этап 6 — Живой прогон и приёмка

**Цель**: клиент видит посты в своих сообществах и принимает работу.

**Что делаем**:
- Промт клиента и его рекламная ссылка в панели.
- Прогон вручную на всех группах, разбор результата: длина постов, обложки, время.
- Переключение `publish_mode` в `live`, расписание.
- Сутки наблюдения: журнал ошибок, число публикаций против плана.

**Критерии приёмки** (закрытие → tag `stage-6-done`):
- [ ] Ручной прогон формирует план на все включённые группы без пересечения тем
- [ ] Посты уходят в ВК и Одноклассники по-настоящему (`publish_mode = live`)
- [ ] Крон отрабатывает сам, без ручного запуска
- [ ] За сутки журнал ошибок чист либо все записи разобраны
- [ ] Клиент подтвердил приёмку
